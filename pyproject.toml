[project]
name = "omarchy-m12"
version = "0.1.0"
description = "Host-local Omarchy customization checkout."
requires-python = ">=3.12"
dependencies = [
    "pyinfra==3.9.2",
]

[tool.uv]
package = false
